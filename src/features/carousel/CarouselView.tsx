import {motion} from "framer-motion"
function CarouselView({images}) {
  return (
    <div className="relativee flex h-full items-center justify-center">
      {images.map((image,imageIndeex) => {
        const index = ()
      })}
    </div>
  )
}

export default CarouselView
